Excellent audit. Now you know exactly what ties you to Lovable. Here's the cleanup plan:

---

The Two Things You CAN'T Just Delete

Item Why
@lovable.dev/vite-tanstack-config This IS your Vite config. Delete it and your app won't build.
LOVABLE_PROJECT_ID in payhero.server.ts Used for PayHero callback URLs. Without it, webhooks break.

---

PROMPT FOR WINDSURF (Complete Lovable Cleanup)

```
I need to completely remove all Lovable dependencies from this project so it can run independently on Vercel. Follow these steps in exact order. Do not skip anything.

== STEP 1: UPDATE META TAGS ==
In src/routes/__root.tsx, replace these Lovable-branded meta tags:
- "Lovable App" → "DukaPOS - Smart Shop Management"
- "Lovable Generated Project" → "Point of Sale & Inventory Management for Kenyan Shop Owners"
- "Lovable" (author) → "DukaPOS"
- "@Lovable" (twitter) → remove the twitter:site tag entirely
- og:title → "DukaPOS - Smart Shop Management"
- og:description → "Point of Sale & Inventory Management for Kenyan Shop Owners"

== STEP 2: FIX ERROR MESSAGES ==
In these 3 files, change "Connect Supabase in Lovable Cloud." to "Connect Supabase in your project settings.":
- src/integrations/supabase/auth-middleware.ts (line ~20)
- src/integrations/supabase/client.server.ts (line ~17)
- src/integrations/supabase/client.ts (line ~16)

== STEP 3: FIX PAYHERO CALLBACK URL ==
In src/lib/payhero.server.ts, find the getCallbackUrl function that references LOVABLE_PROJECT_ID.

Replace the logic with:
- First check for process.env.PAYHERO_CALLBACK_URL
- If that exists, use it
- If not, check for VERCEL_URL environment variable
- If neither exists, throw an error: "PAYHERO_CALLBACK_URL or VERCEL_URL must be set"

Remove ALL references to LOVABLE_PROJECT_ID from this file.

== STEP 4: ADD PAYHERO_CALLBACK_URL TO .env ==
Add this to the .env file:
PAYHERO_CALLBACK_URL=https://your-vercel-domain.vercel.app/api/webhooks/payhero

Replace "your-vercel-domain" with a placeholder. I will update it after deploying to Vercel.

Also add PAYHERO_CALLBACK_URL to .env.example with a placeholder value.

== STEP 5: DELETE LOVABLE FILES ==
Delete the entire .lovable/ directory and all its contents.

== STEP 6: UPDATE package.json ==
Remove "@lovable.dev/vite-tanstack-config" from devDependencies.

After removal, check if there are any other packages with "lovable" in the name. Remove those too if found.

== STEP 7: REPLACE VITE CONFIG ==
This is the most critical step. In vite.config.ts:

Remove this import:
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

Replace with:
import { defineConfig } from "vite";
import { tanstackStart } from "@tanstack/react-start/plugin/vite";

Then update the config to use the standard TanStack Start Vite plugin instead of the Lovable wrapper. The new config should look like:

```typescript
import { defineConfig } from "vite";
import { tanstackStart } from "@tanstack/react-start/plugin/vite";
import react from "@vitejs/plugin-react";
import tsconfigPaths from "vite-tsconfig-paths";

export default defineConfig({
  plugins: [
    tanstackStart(),
    react(),
    tsconfigPaths(),
  ],
});
```

If there are any Lovable-specific comments at the top of the file, remove them.

== STEP 8: REGENERATE LOCK FILE ==
Delete bun.lock (it will be regenerated on next install).

== STEP 9: VERIFY ==
After all changes, check:

· Are there any remaining references to "lovable" (case-insensitive) in any file?
· Are there any remaining references to "LOVABLE_PROJECT_ID"?
· Does vite.config.ts import from standard packages only?

Report what you found and confirm the project is clean.

== RULES ==

· Follow these steps in exact order
· Do NOT skip any step
· Do NOT change anything not listed above
· Do NOT touch PayHero credentials in .env
· Do NOT touch Supabase credentials in .env
· Keep all existing functionality intact

```

---

This completely severs your project from Lovable. Once done, you'll have a clean codebase ready for Vercel deployment. Run it and paste the results.