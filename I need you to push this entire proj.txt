I need you to push this entire project to my GitHub repository. The repository is:
https://github.com/jannesokumu20-alt/shopflow-pos

Currently, this project folder is NOT connected to any Git remote. I need you to:

1. Initialize Git in this folder (if not already done):
   git init

2. Add the remote repository:
   git remote add origin https://github.com/jannesokumu20-alt/shopflow-pos.git

3. Add all files to staging:
   git add .

4. Create a commit:
   git commit -m "Complete cleanup - removed Lovable, added DukaPOS branding, fixed all critical issues"

5. Push to GitHub:
   git push -u origin main

If the push asks for authentication, tell me exactly what credentials it needs and I will provide them.

If "main" branch doesn't work, try "master" instead:
   git push -u origin master

Run these commands one at a time and show me the output of each. If any step fails, stop and tell me the error.