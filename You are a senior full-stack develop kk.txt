You are a senior full-stack developer performing a comprehensive code audit on a production FinTech POS application called DukaPOS. This is a real financial application handling M-Pesa payments via PayHero API in Kenya.

== YOUR JOB ==
Perform a thorough inspection of the ENTIRE codebase. Identify EVERY error, warning, broken import, missing file, type mismatch, missing database table, missing column, or potential runtime failure. Do NOT make any changes yet. Only report what you find.

== PART 1: FULL CODE INSPECTION ==

Check everything listed below:

1. IMPORTS & DEPENDENCIES
- Check every import statement across all files. Are all imported modules installed in package.json?
- Are there any imports pointing to files that don't exist?
- Are any imports unused?
- Check for import path typos

2. COMPONENT CHECKS
- Are all React components properly exported and imported?
- Are all JSX components closed properly?
- Are all hooks (useState, useEffect, etc.) used correctly?
- Are there any components referenced in routes that don't exist?
- Check for missing key props in lists

3. TYPESCRIPT ERRORS
- Find all type mismatches
- Find places where 'any' type is used (flag as potential issues)
- Check for missing type definitions
- Check for incorrect function signatures

4. API & SERVER FUNCTIONS
- Verify all server functions are properly structured
- Check all fetch calls have proper error handling
- Verify API endpoints match between frontend and backend
- Check that all environment variables are properly referenced

5. SUPABASE INTEGRATION
- Check all Supabase queries for correctness
- Verify table names match the actual database schema
- Check Row Level Security policies
- Verify auth flow (register, login, token handling)

6. PAYHERO INTEGRATION
- Check the onboard-till flow in src/lib/payhero.server.ts
- Check the STK push flow for sales
- Check the subscription payment flow
- Verify channel_id usage (platform vs shop owner)
- Check webhook/callback handling — is there a webhook handler file?
- Check for hardcoded IDs or credentials

7. UI/UX ISSUES
- Check for missing icons (Lucide React imports)
- Check for broken Tailwind classes
- Check for missing or incorrect component props
- Verify all buttons have loading states
- Check form validations

8. ROUTING
- Verify all routes are defined correctly
- Check protected routes work
- Verify navigation links point to existing routes

9. ERROR HANDLING
- Check that every API call has error handling
- Verify toast/error messages are user-friendly
- Check for empty states
- Verify loading states exist

10. SECURITY
- Check that no API keys or secrets are exposed in frontend code
- Verify JWT token is sent with authenticated requests
- Check for any hardcoded IDs or credentials

11. CRITICAL MISSING FILES
- Check: is there a webhook handler at src/routes/api/public/webhooks/?
- Check if inventory/stock is decremented after successful sales
- Check subscription expiry logic

== PART 2: DATABASE MIGRATION AUDIT ==

This is critical. I need you to compare what the CODE expects against what the MIGRATIONS actually create.

Step A: Find all migration files
- Look in supabase/migrations/ or any similar directory
- Read every SQL migration file
- List every table, column, index, and policy that the migrations create

Step B: Find what the CODE expects
- Scan every server function and database query in the codebase
- Look for all table names referenced (shops, products, sales, sale_items, subscription_payments, etc.)
- Look for all column names referenced in SELECT, INSERT, UPDATE statements
- Look for all index usage patterns

Step C: Compare and report gaps
For each table the code expects, report:
- TABLE NAME: Is it in the migrations? (✅ Yes / ❌ No)
- MISSING COLUMNS: Columns the code uses but migrations don't create
- MISSING TABLES: Tables the code references but no migration exists
- MISSING INDEXES: Queries that would benefit from indexes that don't exist
- MISSING POLICIES: Tables without Row Level Security policies

Example of what to look for:
- Does the code query `shops.payhero_channel_id`? Is that column in the migration?
- Does the code query `shops.pin_valid_until`? Is that column in the migration?
- Does the code query `sale_items`? Does that table exist in migrations?
- Does the code query `subscription_payments`? Does that table exist?

== HOW TO REPORT ==

Report in this format:

### CODE ISSUES
- FILE: exact file path
- LINE: line number or section
- SEVERITY: 🔴 Critical / 🟡 Warning / 🔵 Minor
- ISSUE: clear description
- FIX: what needs to change

### MIGRATION GAPS
- TABLE: table name
- STATUS: ✅ Complete / ❌ Missing / ⚠️ Incomplete
- MISSING: what's missing (columns, indexes, policies)
- CODE REFERENCES: which files expect this

### SUMMARY
- Total code issues found
- Total migration gaps found
- Critical issues that must be fixed before production

== CRITICAL RULES ==
- DO NOT make any changes to any files
- DO NOT refactor anything
- DO NOT rename anything
- DO NOT add new features
- ONLY inspect and report
- Be thorough. Check every file.
- Pay special attention to: payhero.server.ts, sales.functions.ts, subscription.functions.ts, auth.functions.ts, and the webhooks directory
- Pay special attention to the supabase/migrations/ directory

Start the full inspection and migration audit now. Report everything you find.